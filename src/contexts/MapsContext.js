import { createContext } from "react";

/**
 * Contexto para el manejo de datos geoespaciales.
 * Se encarga de compartir la información de parcelas y capas del mapa entre componentes.
 */
export const MapsContext = createContext();