import { createContext } from 'react'

/**
 * Contexto para la gestión de la autenticación.
 * Almacena el estado del usuario logueado y los métodos relacionados con Firebase.
 */
export const AuthContext = createContext();